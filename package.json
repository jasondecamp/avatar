{
  "name": "@avatar/root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "dev": "lerna exec --scope @avatar/app -- yarn dev",
    "start": "lerna exec --scope @avatar/app -- yarn dev"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "dependencies": {}
}
